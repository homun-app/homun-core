# Riferimento UX

Copia dei sorgenti precedenti alla separazione della nuova app. I punti di ingresso sono `prototypes/conversations.tsx` e `prototypes/first-work.tsx`; il server usa `vite.prototype.config.ts` dalla radice.

Questa copia serve da riferimento visivo e funzionale. Le nuove funzionalità applicative vanno in `apps/web`, non in questa cartella. Configurazioni originali conservate per consultazione; dipendenze e risorse pubbliche restano alla radice.
