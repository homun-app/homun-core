# Homun handoff - 2026-07-31

> **Doc reset 2026-07-31:** le mappe operative sono in [`README.md`](README.md),
> [`STATO.md`](STATO.md) e [`architecture/`](architecture/) (riscritte dal codice).
> Il materiale storico e' in [`archive/2026-07-31-doc-reset/`](archive/2026-07-31-doc-reset/).
> `CLAUDE.md` e' stato rimosso. Usa questo file solo come **prompt lungo** per un
> altro agent; non come inventario di documenti da rileggere tutti.

Questo documento e' intenzionalmente autosufficiente, ma impone di verificare il
codice corrente prima di fidarsi di qualsiasi affermazione.

## Prompt pronto da usare

```text
Lavora come senior Rust/Electron engineer sul repository Homun:

  /Users/fabio/Projects/Homun/app

Obiettivo generale
------------------
Portare Homun da candidato di sviluppo stabile a release candidate collaudata.
Non aggiungere altri agent loop, scheduler, protocolli HITL, store, projector o
wrapper paralleli. Contratto unico: ExecutionContract -> execute -> ExecutionOutcome.

Stato Git
---------
Verifica subito (non assumere SHA):

  pwd && git status --short && git branch --show-current
  git log -20 --oneline --decorate && git diff

Baseline funzionale UI nota: 9d2788e8. Versione prodotto in
apps/desktop/package.json (0.1.1094 al handoff). Preserva modifiche non tue.
Niente reset distruttivi, push, tag, publish senza richiesta esplicita.

Documenti da leggere (solo questi)
----------------------------------
1. docs/README.md
2. docs/STATO.md
3. docs/CAPISALDI.md
4. docs/architecture/ (overview, agent-loop, execution, memory, desktop-ui, runtime-flags)
5. docs/decisions/ solo se serve il "perche'" di una scelta (ADR immutabili)
6. docs/testing/release-candidate-matrix.md prima di una RC

NON leggere come specifica: docs/archive/** (incluso 2026-07-31-doc-reset).

Regola di verita'
-----------------
Codice, migrazioni SQLite, test e tracce runtime > documenti. Ricostruisci
journal / outbox / receipt / turn events / task-run-message e stato terminale
canonico — non fidarti della sola UI.

Cosa e' gia' nel codice (non reimplementare)
-------------------------------------------
- Loop unico engine::run_turn; ExecutionHost + EffectHost + projection outbox
- Receipt Uncertain senza auto-rerun; long-running lease/park/resume/cancel
- Browser sidecar checkpoint/generation; HITL unico; Tasks UI rimossa
- SandboxMode; memoria pool ON / service OFF; UI grammatica fase 1
- Dettaglio: docs/architecture/*

Lavoro residuo
--------------
P0: gate (fmt, clippy -D warnings, pre_release_gate, audit) + collaudo Electron
    reale con verifica persistenza (turni, wait, cancel, browser, approval,
    uncertain, sandbox, Vault, memoria, connector, presentazioni, automazioni).
P1: rifinitura UI dettaglio; poi RC multipiattaforma draft (no publish).

Metodo
------
TDD su bug/contratti; un owner per lifecycle/policy/receipt/projection;
modello propone, runtime autorizza; commit piccoli; niente Co-Authored-By.
```

## Nota

Se HEAD e' avanzato dopo il handoff, aggiorna SHA / conteggi test / gap in
`STATO.md` prima di modificare codice.
