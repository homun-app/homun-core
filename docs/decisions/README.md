# Decisions (ADR)

Record **immutabili** del “perché”. Non sono lo stato corrente del codice:
dopo un ADR Accepted, verifica sempre crate/simbolo in tree.

Ingresso operativo: [`../STATO.md`](../STATO.md) · mappa as-built:
[`../architecture/`](../architecture/).

Numerazione: `0001` … `0028` (ultimo: retire mistralrs unwired).
